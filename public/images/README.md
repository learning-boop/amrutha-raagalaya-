Place the academy's real photographs here (JPG/WebP, ~1600px wide max) and reference them
from `src/lib/content.ts` (gallery `src`) or pass `src="/images/..."` to the `<Photo>` component.
Get written consent for every child pictured; do not put children's names on images.
